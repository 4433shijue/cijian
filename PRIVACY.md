# 数据与隐私说明

此间是浏览器直接连接模型服务的网页应用。公开托管网页不等于公开每位访客的存档，也不代表内容完全不离开设备。

## 哪些内容保存在设备上

角色、创建角色时的自动草稿、故事、当前一轮灵感建议、聊天、记忆、提示词设置和接口配置保存在当前浏览器的 IndexedDB 中。新手引导表单另外使用 localStorage 暂存进度，暂存内容不包含 API Key。应用没有账号系统、云端存档服务器或访客内容管理后台。

API Key 默认只保存在当前页面的运行内存中，刷新后需要重新填写。勾选「记住此设备的 Key」后，密钥会以未加密形式写入浏览器数据库；这不是密码保险箱。能访问这台设备或在同一网站运行代码的人员、扩展程序仍可能读取数据。公共设备上不要勾选记住。

存储跟随浏览器和网站来源。不同设备、浏览器、协议、域名或端口之间不会自动同步。同一域名下的不同路径不构成浏览器安全隔离；同一个 GitHub 账号下的多个项目站点可能共享来源，数据库命名不能提供安全隔离。需要独立隔离时应使用单独的域名或子域名。

清理网站数据、关闭隐私窗口或浏览器回收存储可能移除存档。请定期导出备份，用备份在本地版与在线版之间迁移。

## 哪些内容会发给模型服务

只有执行测试连接、获取模型列表、生成正文、请求灵感、聊天或整理记忆等操作时，应用才向所填写的模型接口发送相应请求。API Key 用于这些请求的鉴权。生成请求会包含本次任务需要的输入、提示词，以及按上下文规则选取的角色、故事、聊天或记忆内容。灵感小助手使用最近指定段数的完整正文、当前故事人物与适用的世界书，也会参考尚未扩写的输入；点击「你再想想」时会附上上一轮建议以避免重复。重新打开已保存的一轮建议不会再次请求模型。

模型服务及其上游可能记录和处理请求，具体取决于所选服务的政策。角色知情过滤是生成逻辑，不能阻止模型服务看到实际收到的内容。请核实接口地址，使用可信的 HTTPS 服务，并了解其数据使用方式。

生成请求可携带服务支持的短期提示词缓存标记，用于复用固定世界书和人物设定。缓存由你选择的模型服务处理，保留方式与有效期取决于服务政策。可在 API 高级设置关闭显式标记；服务自身的自动缓存仍由服务管理。「检查并采用」只在本机保存你确认的正文，不会因此额外调用模型。

每位访客使用自己的密钥和额度。静态网页不能安全隐藏一个供所有访客共用的模型密钥。不要把真实密钥写入源码、构建环境变量、网页配置或公开文件。浏览器直连还要求服务允许站点来源的 CORS 请求。

## 公开网站和源码意味着什么

GitHub Pages 提供网页文件。网站的脚本、样式、图片和其中写入的固定内容都可以被访问者下载；公开仓库还会公开源文件和提交历史。当前应用没有接入第三方统计、广告、远程字体或自动上传存档的功能。GitHub 等托管服务仍会处理访问请求及其日志，这与应用存档是不同的数据。

只上传经过审核的程序、素材、虚构测试数据和部署文件。不要上传 `.env`、本地工作记录、真实人物私密档案、接口凭据或浏览器导出的备份。

导出备份会去除接口配置中的 Key 和请求上下文缓存，但保留完整的角色、剧情、聊天、记忆、接口地址与自定义提示词。如果你把密钥或其他隐私手动写进这些正文或接口地址，备份不会自动识别并删除。备份适合自己保存；分享之前需要逐项检查。

## 参考

- [GitHub Pages 与 HTTPS](https://docs.github.com/en/pages/getting-started-with-github-pages/securing-your-github-pages-site-with-https)
- [GitHub Pages 介绍及访问数据](https://docs.github.com/en/pages/getting-started-with-github-pages/about-github-pages)
- [浏览器同源策略与存储范围](https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Same-origin_policy)
